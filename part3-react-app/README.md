React app for EE461L HW4, task 3.

To run it:

1. Run `npm install`
2. Run `npm run dev`

To submit it:

1. Zip the whole `part3-react-app` folder
2. Leave out `node_modules`
3. Submit the zip in Canvas

The PDF says the last commit time counts as the turn-in time.
